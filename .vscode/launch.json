{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach",
            "port": 9229,
            "request": "attach",
            "cwd": "${workspaceFolder}",
            "type": "node",
            "sourceMaps": true,
            "outFiles": [
                "${workspaceFolder}/server/dist/**/*.js"
            ],
            "remoteRoot": "/app/dist",
            "localRoot": "${workspaceFolder}/server/dist"
        }
    ]
}